apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-health-probes
  namespace: n8n
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    # Allow kubelet health probes from any source
    - ports:
        - protocol: TCP
          port: 5678
